{
  "name": "jynta",
  "version": "0.1.0",
  "private": true,
  "description": "Jynta — One AI. One System. Unlimited Possibilities.",
  "workspaces": [
    "apps/*",
    "packages/*",
    "services/*",
    "database",
    "tools"
  ],
  "scripts": {
    "dev": "npx turbo run dev",
    "build": "npx turbo run build",
    "lint": "npx turbo run lint",
    "type-check": "npx turbo run type-check",
    "test": "npx turbo run test",
    "clean": "npx turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.1.0",
    "typescript": "^5.6.0",
    "@biomejs/biome": "^1.9.0"
  }
}
